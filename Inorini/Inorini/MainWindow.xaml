<Window x:Class="Inorini.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Inorini"
        
        xmlns:csharplib="clr-namespace:Csharplib.winTitleBar;assembly=vsResources" 

        Title="Mollbayasee" Height="347" Width="529"
        WindowStartupLocation="CenterScreen"
        Background="#FF1A1A1D" Foreground="White" WindowStyle="None" AllowsTransparency="True" FontFamily="Segoe UI" 
        >

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="file:///D:/Workspace/GitHub/Csharplib/project/vsResources/vsResources/Themes/Generic.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Border BorderBrush="{Binding Background, ElementName=Debugtag}" BorderThickness="1">

        <Grid>

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <csharplib:winTitleBar Title="{Binding Title, RelativeSource={RelativeSource AncestorType={x:Type Window}}}" Icon="{Binding Icon, RelativeSource={RelativeSource AncestorType={x:Type Window}}}"/>
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="2*"/>
                    </Grid.ColumnDefinitions>

                    <ListBox x:Name="HeadListBox" SelectionChanged="HeadListBox_SelectionChanged" Margin="0,0,0,0"/>
                    <Grid Grid.Column="1" >
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <DataGrid x:Name="main_data" 
                            AutoGenerateColumns="False"
                            HeadersVisibility="Column"
                            CanUserAddRows="False"
                            CanUserDeleteRows="False"
                            CanUserReorderColumns="False"
                            CanUserResizeColumns="True"
                            CanUserResizeRows="False"
                            CanUserSortColumns="False"
                            IsReadOnly="True"
                            SelectionChanged="Main_data_SelectionChanged">
                            <!-- 改为 False 允许编辑 -->
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="键" Width="*" Binding="{Binding Key}" IsReadOnly="True"/>
                                <DataGridTemplateColumn Header="值" Width="*">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding Value}" VerticalAlignment="Center"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                    <DataGridTemplateColumn.CellEditingTemplate>
                                        <DataTemplate>
                                            <TextBox Text="{Binding Value, UpdateSourceTrigger=PropertyChanged}" 
                                                VerticalAlignment="Center"
                                                BorderThickness="0"
                                                Background="Transparent"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellEditingTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                        <Grid x:Name="数据增加功能区" Grid.Row="1" Visibility="Collapsed">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Label Content="键对"/>
                            <UniformGrid Columns="2" Rows="1" Grid.Column="1" Margin="0,2.5,0,2.5">
                                <TextBox x:Name="增加键" Margin="0,0,0,0" Text="Key"/>
                                <TextBox x:Name="增加值" Margin="2.5,0,0,0" Text="Value"/>
                            </UniformGrid>
                            <Button x:Name="增加" Content="增加" IsEnabled="False" Click="AddButton_Click" Grid.Column="2" Margin="2,2,0,2"/>
                        </Grid>
                    </Grid>

                </Grid>

                <StackPanel Grid.Column="1">
                    <Button Content="编辑" Click="EditButton_Click"/>
                    <Button x:Name="删除" Content="删除" IsEnabled="False" Click="DelButton_Click"/>

                </StackPanel>
                
            </Grid>
            <Label x:Name="Debugtag" VerticalContentAlignment="Center" 
                Content="就绪" 
                Background="#FF007ACC" Padding="5" Grid.Row="2" ToolTip="{Binding Content, RelativeSource={RelativeSource Mode=Self}}"/>
        </Grid>
    </Border>
</Window>
