<Window x:Class="Mollbayasee.winDumpbin"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Mollbayasee"
        mc:Ignorable="d"
        
        xmlns:Ramitta="clr-namespace:Ramitta;assembly=Ramitta" 


        Title="Mollbayasee - Dumpbin" Height="410" Width="578"
WindowStartupLocation="CenterScreen"
Background="#FF1A1A1D" Foreground="White" WindowStyle="None" AllowsTransparency="True" FontFamily="Segoe UI" Icon="/森林.png" 
    >

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Ramitta;component/Generic.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Ramitta:winTitleBar Title="{Binding Title, RelativeSource={RelativeSource AncestorType={x:Type Window}}}" Icon="{Binding Icon, RelativeSource={RelativeSource AncestorType={x:Type Window}}}"/>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="msg_result" 
                 IsReadOnly="True" 
                 VerticalScrollBarVisibility="Auto" 
                     HorizontalScrollBarVisibility="Auto"
                 Margin="0,0,0,0" 
                 TextWrapping="Wrap"/>

            <DataGrid x:Name="func_data" Grid.Column="1" 
          AutoGenerateColumns="False"
          HeadersVisibility="Column"
          CanUserAddRows="False"
          CanUserDeleteRows="False"
          CanUserReorderColumns="False"
          CanUserResizeColumns="True"
          CanUserResizeRows="False"
          CanUserSortColumns="False"
          IsReadOnly="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="序号" Width="*" Binding="{Binding Index}"/>
                    <DataGridTextColumn Header="函数" Width="*" Binding="{Binding Func}"/>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>


        <Label x:Name="Debugtag" VerticalContentAlignment="Center" Content="就绪" 
Background="#FF007ACC" Padding="5" Grid.Row="2" ToolTip="{Binding Content, RelativeSource={RelativeSource Self}}"/>
    </Grid>
</Window>
