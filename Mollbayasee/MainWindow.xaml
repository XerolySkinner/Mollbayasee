<Window x:Class="Mollbayasee.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Mollbayasee"
        mc:Ignorable="d"
        
        xmlns:csharplib="clr-namespace:Csharplib.winTitleBar;assembly=winTitleBar" 

        
        Title="Mollbayasee" Height="108" Width="260"
        WindowStartupLocation="CenterScreen"
        Background="#FF1A1A1D" Foreground="White" WindowStyle="None" AllowsTransparency="True" FontFamily="Segoe UI" Icon="/森林.png" 
    >

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="vsResources.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Border BorderBrush="{Binding Background, ElementName=Debugtag}" BorderThickness="1">

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <csharplib:winTitleBar Title="{Binding Title, RelativeSource={RelativeSource AncestorType={x:Type Window}}}" Icon="{Binding Icon, RelativeSource={RelativeSource AncestorType={x:Type Window}}}"/>
            <Grid Grid.Row="1" >
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Button Content=".Net类型dll" Click="Net类型dll_Click"/>
                <Button Content="常规dll" Grid.Column="1" Click="Button_Click"/>
                <Button Content="可执行文件" Grid.Column="2"/>
            </Grid>
            <Label x:Name="Debugtag" VerticalContentAlignment="Center" 
   Content="就绪" 
   Background="#FF007ACC" Padding="5" Grid.Row="2" ToolTip="{Binding Content, RelativeSource={RelativeSource Self}}"/>
        </Grid>
    </Border>
</Window>
